const initialState = {
  data: {
    breakfast: [1, 2, 3],
    lunch: [4, 5, 6],
  },
}

export default function meals(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
